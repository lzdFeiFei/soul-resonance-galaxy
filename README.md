# Soul Resonance React

> 灵感与情绪的共鸣宇宙 - React 重构版本

## 项目概述

Soul Resonance 是一个极简主义的情绪可视化交互应用，将用户的思绪转化为粒子流场和声波动态。本项目是对原版 Vanilla JS 实现的完整 React + TypeScript 重构。

## ✨ 核心特色功能

### 已完成功能
- **🌌 粒子流场系统**：400个粒子基于伪噪声函数的自然漩涡流动
- **🎵 16根动态声波**：Header 右侧正弦波叠加算法音频可视化
- **🔄 3阶段状态流转**：输入 → 共鸣(Loading+Result) → 星系漫游
- **💎 精致视觉设计**：液态玻璃质感、极细线条、坍缩动画
- **🎨 完美色彩系统**：Soul Gold、深空黑背景、噪声纹理
- **⚡ 现代状态管理**：Zustand 5.0 + TypeScript 严格模式

### 开发中功能
- **🌟 星系漫游系统**：黄金角度螺旋布局 + 智能避让逻辑
- **👂 回声面板交互**：侧滑面板 + 嵌入粒子动画
- **🎭 高级动画效果**：摄像机平滑移动、坍缩保存效果

## 🚀 技术栈

- **React 19** + TypeScript + Vite
- **Tailwind CSS** + 自定义设计系统
- **Zustand 5.0** 状态管理
- **Canvas API** + 自定义粒子物理引擎
- **Framer Motion** 动画增强

## 📁 项目结构

```
src/
├── components/           # UI组件层
│   ├── layout/          # Header, NoiseOverlay
│   ├── canvas/          # ParticleCanvas, HeaderWave
│   ├── stages/          # InputStage, LoadingStage, ResultStage
│   └── ui/              # GlassButton, TypewriterText
├── hooks/               # 业务逻辑层
│   ├── useParticleSystem.ts
│   └── useWaveAnimation.ts
├── stores/              # 状态管理 (Zustand)
│   ├── appStore.ts      # 全局应用状态
│   ├── galaxyStore.ts   # 星系数据管理
│   └── canvasStore.ts   # Canvas 交互状态
├── utils/               # 工具函数
│   ├── particlePhysics.ts
│   └── constants.ts
├── types/               # TypeScript 类型
└── styles/              # 全局样式
```

## 🎯 核心算法

### 粒子流场物理引擎
```typescript
// 基于多层正弦/余弦噪声的自然流动算法
const noise1 = Math.sin(x * scale + time * speed) * Math.cos(y * scale + time * speed);
const noise2 = Math.sin(x * scale * 2 + time * speed * 1.5) * Math.cos(y * scale * 2 + time * speed * 1.5);
const combinedNoise = (noise1 * 0.6) + (noise2 * 0.3) + (noise3 * 0.1);
```

### 16根声波算法
```typescript
// 高-低-高节奏 + 正弦波叠加 + 金色灰色交替
const animatedHeight = baseHeight + Math.sin(phase) * 0.3 + Math.sin(phase * 1.5) * 0.2;
ctx.fillStyle = i % 2 === 0 ? soulGold : mutedGrey;
```

## 🎨 设计规范

### 色彩系统
- **背景**: 深空黑 `#050605`
- **主色**: Soul Gold `rgba(210, 190, 160, 0.9)`
- **辅色**: Muted Grey `rgba(143, 150, 147, 0.4)`
- **点缀**: Soul Green, Purple, Blue, Pink

### 动画标准
- **坍缩效果**: `scale(0.92) + blur(5px)`，800ms
- **阶段切换**: 800ms ease-in-out
- **打字机**: 50ms/字符

## 📊 开发进度

- [x] **Phase 1**: 基础架构 React + TS + Tailwind
- [x] **Phase 2**: 状态管理 Zustand stores 
- [x] **Phase 3**: 粒子系统 Canvas + 物理引擎
- [x] **Phase 4**: 核心界面 Input + Loading + Result
- [ ] **Phase 5**: 星系系统 Galaxy + Camera (50% 完成)
- [ ] **Phase 6**: 回声面板 Echo Panel + 侧滑动画
- [ ] **Phase 7**: 性能优化 + 细节打磨

## 🛠 开发指南

### 启动开发
```bash
npm install
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 代码检查
```bash
npm run lint
```

### 项目特色

#### 完美还原原版
- ✅ 每个像素、每帧动画、每个交互细节都严格按原版规范
- ✅ 粒子物理算法完全一致
- ✅ 声波动画节奏和颜色精确匹配
- ✅ 液态玻璃质感和噪声纹理效果

#### 现代 React 架构
- ✅ 组件化设计，高度可维护
- ✅ TypeScript 严格类型安全
- ✅ Zustand 现代状态管理
- ✅ 自定义 Hook 业务逻辑封装

#### 性能优化
- ✅ Canvas 渲染循环优化
- ✅ requestAnimationFrame 流畅动画
- ✅ 状态更新防抖优化
- ✅ 组件懒加载准备

## 🔮 后续计划

### 即将实现
- **星系漫游完整功能**：黄金角度螺旋布局算法
- **回声面板系统**：侧滑动画 + 嵌入粒子效果
- **高级交互逻辑**：摄像机平滑移动、粒子点击检测

### 未来扩展
- **AI 集成**: OpenAI/Claude API 真实内容生成
- **音频生成**: MusicGen 情绪音频合成
- **数据持久化**: Firebase/Supabase 云端同步
- **多用户交互**: 实时在线用户星系

---

**当前状态**: 核心功能完成 70%，可运行和测试基础交互流程

**启动地址**: http://localhost:5173/ (开发服务器已启动)